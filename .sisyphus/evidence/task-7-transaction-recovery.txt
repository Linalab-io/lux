Task 7 transaction recovery evidence

Implemented and verified by `gateway/tests/lux_lifecycle_test.rs::transaction_recovery_commits_planned_lifecycle_writes_on_start`.

Covered behavior:
- Transaction journals live under `.lux/runs/<run_id>/transactions/<uuid>.json`.
- Planned write operations capture pre-image state, apply atomic file writes, and can be committed later.
- Gateway startup calls `recover_pending_transactions` from `GatewayState::new`.
- Recovery scans planned journals, reapplies idempotent operations, marks the journal `committed`, and leaves run-state Completed plus roadmap Pushed.

Verification: `cd gateway && cargo test` passed.
