Task 7 verification failure evidence

Implemented and verified by `gateway/tests/lux_lifecycle_test.rs::verification_failure_creates_blocker_and_blocks_push_without_evidence`.

Covered behavior:
- A failing T3-style verification result creates a stable bounded blocker ticket through the existing blocker graph.
- The active ticket is marked `Blocked` with the blocker dependency.
- Missing T3 evidence prevents milestone push.
- Roadmap phase is not marked `Pushed` and run-state is not completed on failure.

Verification: `cd gateway && cargo test` passed.
