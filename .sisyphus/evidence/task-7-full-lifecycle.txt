Task 7 full lifecycle evidence

Implemented and verified by `gateway/tests/lux_lifecycle_test.rs::full_lifecycle_awaits_approval_then_pushes_and_completes`.

Covered order:
1. Run state enters Planning for selected milestone.
2. Task DAG ticket moves AwaitingEvidence then Done only with evidence.
3. `begin_milestone_push_approval` requires existing T3 evidence and sets `AwaitingApproval` with `approval.gate=ApproveDiff`, `approval.pending_transition=milestone_push`, and `approval.awaiting_since`.
4. Approved push uses mocked git push runner.
5. Transaction journal writes run-state and `.lux/roadmap.json` atomically.
6. Roadmap phase becomes `Pushed` with evidence path and git SHA.
7. Run state becomes `Completed` with `stop_reason=milestone_complete`.

Verification: `cd gateway && cargo test` passed.
